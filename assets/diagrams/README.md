# Diagrams

Semantic diagrams used directly by rich scenes: SVG/PNG graph fragments, ER diagrams, proof/logic diagrams, workflow diagrams, and similar visual objects.

Prefer SVG when the diagram is vector-native and interactive styling is useful. Keep editable source formats elsewhere unless the website consumes them directly.
