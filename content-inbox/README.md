# Content inbox

Temporary staging area for material that still needs classification before it becomes part of the portfolio data model.

Nothing in this directory should be referenced by the public site directly. Once a link or file is classified, move it into the appropriate `assets/` location or register it as an external artifact in `artifact-data.js`, then remove it from the inbox.
